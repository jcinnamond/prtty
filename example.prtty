.clear
.vcenter << .center << .type << this is a title slide
.wait

.clear
.vcenter 
    hello
    .nl
    this is some content
    .nl
    vcentered
.wait

.clear
.margin[top=1/3;left=1/5]
.vcenter
    centered with margins
    .nl
    over multiple lines
.wait

.clear
.margin[left=2]
.vcenter
    centered
    .nl
    .wait
    .type << with a small margin
.wait

.clear
new text 
.wait

even more text
.wait

.nl
over another line
.wait