module Runtime.Instructions (
    Instruction (..),
    Anchor (..),
    Style (..),
    nullStyle,
    emptyStyle,
) where

import Runtime.Internal.Types (Anchor (..), Instruction (..), Style (..), emptyStyle, nullStyle)
